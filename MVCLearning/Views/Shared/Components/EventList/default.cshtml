@using MVCLearning.Models
@model IEnumerable<Event>
<ul>
    @foreach(var ev in Model)
    {
        <li>
            <div>
                @ev.Day.ToString("D")
            </div>
            <div>
                @ev.Text
            </div>
        </li>
    }
</ul>
